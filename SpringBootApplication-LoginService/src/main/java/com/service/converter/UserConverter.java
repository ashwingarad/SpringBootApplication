package com.service.converter;

public class UserConverter {

    /*public static UserMaster fromUserLoginFormToUserLoginMaster(UserForm userForm) {
        UserMaster userMaster = null;
        if (userForm != null) {
            userMaster = new UserMaster();
            if (userForm.getId() != null)
                userMaster.setId(userForm.getId());

            userMaster.setUsername(userForm.getUsername());
            userMaster.setPassword(userForm.getPassword());
        }
        return userMaster;
    }

    public static UserForm fromUserLoginMasterToUserLoginForm(UserMaster userMaster) {
        UserForm userForm = null;
        if (userMaster != null) {
            userForm = new UserForm();
            if (userMaster.getId() != null)
                userForm.setId(userMaster.getId());

            userForm.setUsername(userMaster.getUsername());
            userForm.setPassword(userMaster.getPassword());
        }
        return userForm;
    }*/
}
